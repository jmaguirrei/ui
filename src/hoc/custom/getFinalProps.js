export function getFinalProps(args) {  const {    store,    componentDef,    classNamesMapping,    processedStyles,  } = args;  const {    state = () => ({}),    actions = () => ({}),  } = componentDef;  const classNames = {    ...Object.keys(classNamesMapping).reduce((acum, key) => {      return { ...acum, [key]: classNamesMapping[key] };    }, {}),  };  return (props = {}) => {    return {      props,      state: state(props, store),      actions: actions(props, store),      styles: processedStyles,      classNames,    };  };}