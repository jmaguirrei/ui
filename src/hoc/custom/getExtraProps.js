export function getExtraProps(store, componentDef) {  const {    state = () => ({}),    actions = () => ({}),  } = componentDef;  return (props, processedStyles) => {    const { classStylesMapping, inlineStylesRules } = processedStyles;    const styles = {      ...Object.keys(classStylesMapping).reduce((acum, key) => {        return { ...acum, [key]: classStylesMapping[key] };      }, {}),      ...inlineStylesRules,    };    return {      state: state(props, store),      actions: actions(props, store),      styles,    };  };}