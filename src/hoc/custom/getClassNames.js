import { processStyle } from '../../styles';export function getClassNames(registerNumber, componentStyles = {}) {  const styleKeys = Object.keys(componentStyles);  const mapping = {};  const rules = styleKeys.reduce((acum, key) => {    mapping[key] = `${key}--${registerNumber}`;    return `${acum}${processStyle(key, componentStyles[key])}`;  }, '');  return {    rules,    mapping,  };}