/*  Apply inline styles to elements from template literales "beautiful" line breaks*/export function inlineStyle(fixed, ...dynamic) {  const iStyleStr = fixed.reduce((acum, value, i) => {    const cleanValue = value.replace(/;\s+/g, '; ').trim();    if (i === fixed.length - 1) return `${acum}${cleanValue}`;    return `${acum}${cleanValue} ${dynamic[i]}`;  }, '');  if (process.browser) return iStyleStr;  // Server-side we need the colons ''  return `'${iStyleStr}'`;}