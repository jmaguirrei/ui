import stylis from 'stylis';// stylis minify CSS and add vendor prefixesexport function registerStyles(registerNumber, styles, componentStylesString) {  if (!styles) return [];  const classNamesMapping = Object.keys(styles).map(className => {    const newClassName = `${className}--${registerNumber}`;    return {      className,      newClassName,    };  });    console.log("classNamesMapping", classNamesMapping);  /* eslint-disable no-param-reassign */  componentStylesString = classNamesMapping.reduce((acum, item) => {    const cssRules = styles[item.className];    const cssRulesStyled = stylis(`.${item.newClassName}`, cssRules);    return `${acum}${cssRulesStyled}`;  }, componentStylesString);    console.log("componentStylesString", componentStylesString);  return {    classNamesMapping,    componentStylesString,  };}