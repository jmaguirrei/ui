import stylis from 'stylis';// stylis minify CSS and add vendor prefixesexport function processStyle(key, rules) {  if (key) return stylis(`.${key}`, rules);  // Inline styles does not need the class selector  const out = stylis('', rules);  return out.substr(1, out.length - 2);}