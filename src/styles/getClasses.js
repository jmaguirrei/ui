import { processStyle } from './processStyle';export function getClasses(registerNumber, componentDef) {  const classKeys = Object.keys(componentDef.classes);  const mapping = {};  const rules = classKeys.reduce((acum, key) => {    const newClassName = `${key}--${registerNumber}`;    mapping[key] = newClassName;    return `${acum}${processStyle(newClassName, componentDef.classes[key])}`;  }, '');  return {    rules,    mapping,  };}